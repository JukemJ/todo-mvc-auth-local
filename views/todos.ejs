<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Images!</title>
    <link rel="stylesheet" href="css/style.css">
    <script src="https://kit.fontawesome.com/847f5342f9.js" crossorigin="anonymous"></script>
</head>
<body>
    <h1>Todos</h1>
    <div id="slide-container">
    <ul class="slider">
        <% todos.forEach( el => { %>

                <li class='todoItem slide' data-id='<%=el._id%>'>
                    <span class='<%= el.completed === true ? 'completed' : 'not'%>'><img src='<%= el.todo %>'></span>
                    <span class='del'> 🗑️ </span>
                </li>

        <% }) %>
        <button class="btn btn-next"><i class="fa-solid fa-arrow-right"></i></button>
        <button class="btn btn-prev"><i class="fa-solid fa-arrow-left"></i></button>
    </ul>
    </div>

    

    <h2><%= user.userName %> has <%= left %> <%=`${left == 1 ? 'thing' : 'things'}`%> left to do.</h2>

    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter image url" name='todoItem'>
        <input type="submit" value="Add image">
    </form>

    <a href="/logout" id='logout' >Logout</a>

    <script src="js/main.js"></script>
</body>
</html>